# JWT Configuration
JWT_SECRET=your-jwt-secret-key-change-in-production

# Redis Configuration (Choose one)
# Option 1: Upstash Redis (Recommended for production)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-redis-token

# Option 2: Local Redis (For development only)
# REDIS_URL=redis://localhost:6379

# Sentry Configuration (Optional - for error tracking)
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000

# Notification System Configuration
# Production API URL for notifications and WebSocket
NEXT_PUBLIC_API_BASE_URL=https://backend.worldvisa-api.cloud

# API Configuration
# Zoho DMS API base URL
NEXT_PUBLIC_ZOHO_BASE_URL=https://backend.worldvisa-api.cloud/api/zoho_dms

# CI/CD Configuration (Automatically set in CI environments)
# CI=true
