/** Gray panel fill — `UsersCard`, collapsed `DeadlineExtensionOutcomeCard`. */
export const DEADLINE_EXPANDED_PANEL_GRAY = "#f5f5f5";

/** White stat card + expanded outcome card — full rounding; sits above gray outcome with `gap-1`. */
export const DEADLINE_INNER_CARD_RADIUS_PX = 16;

/** Layered shadow for white elevated inner cards (stat, expanded reason). */
export const DEADLINE_WHITE_CARD_SHADOW =
  "0px 4px 8px -2px rgba(51,51,51,0.06)," +
  "0px 2px 4px 0px rgba(51,51,51,0.04)," +
  "0px 1px 2px 0px rgba(51,51,51,0.04)," +
  "0px 0px 0px 1px #f5f5f5";
