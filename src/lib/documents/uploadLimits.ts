/** Maximum size per individual file (5 MB) */
export const MAX_FILE_SIZE_BYTES = 5 * 1024 * 1024;

/** Maximum total batch size (50 MB) */
export const MAX_TOTAL_SIZE_BYTES = 50 * 1024 * 1024;

/** Default per-upload file ceiling when no template allowedDocument is set */
export const DEFAULT_MAX_FILES = 10;
